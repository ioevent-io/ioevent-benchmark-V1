spring:
  kafka:
    bootstrap-servers: localhost:29092
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ProductsCamunda
  datasource:
    url: jdbc:mysql://localhost:3306/camunda
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
camunda.bpm.admin-user:
  id: demo
  password: demo